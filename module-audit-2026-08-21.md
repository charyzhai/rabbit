# 兔兔英语闯关：全模块功能巡检与优化建议

**巡检日期：** 2026-08-21  
**范围：** 首页、地图闯关、复习、新增词分组、角色对话、词义配对、家长模式、报告、离线音频与本地学习数据。  
**结论：** 应用的核心学习闭环已经具备可用基础：类型检查通过，47 项单元测试通过，最近一次 Android 静态导出通过。不过，当前仍有一项会使静态检查失败的 React Hook 问题，以及几处会让“错词—复习—家长报告”数据链路不完全一致的风险。建议先完成稳定性与数据一致性修复，再扩展新的玩法或继续大规模加词。

## 1. 当前整体状态

| 维度 | 结论 | 说明 |
|---|---|---|
| 核心学习流程 | 基本可用 | 首页、地图、关卡、词义配对、材料词练习、微故事、复习、家长报告与离线音频均有可访问入口。 |
| 自动化验证 | 良好但未闭环 | TypeScript 与 47 项单元测试通过；`pnpm lint` 目前有 1 项错误和 26 项警告，暂不能作为严格 CI 门槛。 |
| 新增词练习 | 已改善 | 内容馆已显示总词数、分组数、完成组数，并可继续下一未完成组或从首组复练。 |
| 家长与本地安全 | 基础完整 | PIN、锁定策略、生物识别开关、加密备份、同步码与操作日志均已有实现。 |
| 数据一致性 | 需要优先加强 | 不同模块对错词、学习时长、正确率和复习计划的写入规则不统一。 |

## 2. 建议优先修复的问题

| 优先级 | 问题 | 影响与复现线索 | 建议处理方式 |
|---|---|---|---|
| P0 | 主闯关页存在条件式 Hook | `app/lesson/[id].tsx` 在 `lesson` 不存在时提前返回，之后才调用 `useEffect`。静态检查已报错；路由参数异步变化或异常恢复时存在 Hook 顺序风险。 | 将所有 Hook 移到任何提前返回之前；在副作用内部用 `if (!lesson) return` 保护，并处理自动跳题定时器的依赖。 |
| P0 | 错词进入复习的路径不一致 | 主题关错词会进入间隔复习计划；词义配对错词仅进入弱词池；新增词短练习和角色对话答错后通常只允许重试，未统一写入复习计划。复习页只显示到期计划，因此可能出现“报告显示待巩固词，但复习页没有今日任务”。 | 建立统一的 `recordLearningMistake` 入口：所有可判定错误都同时更新弱词池和间隔复习计划，并保留来源、日期与错误次数。 |
| P0 | “今日”日期按 UTC 计算 | `dateKey()` 与周趋势使用 `toISOString()`。在中国时区的零点至早上八点，学习记录会被记入前一天，影响连续学习、提醒、日报与周报。 | 使用设备本地年月日生成日期键，并统一改造周统计的日期偏移逻辑；补充跨零点与周末边界测试。 |
| P1 | 首页题量文案固定为“4 道题” | G1—G6 已扩展为 6 道题，但首页仍显示“4 道题”，会造成预期不一致。 | 用 `nextLesson.questions.length` 动态显示真实题量。 |
| P1 | 新增词与故事学习未完整进入家长统计 | 材料词主要记录技能次数，微故事与阅读完成也未统一累加到 `dailyRecords` 的时长、题数与正确率。家长报告会低估这些模块的真实学习投入。 | 定义统一 `StudyEvent` 记录：模块、时长、题数、正确数、完成状态、词汇 ID；所有玩法写同一事件流，再由报告聚合。 |
| P1 | 对话和词义配对缺少真正的结束页 | 角色对话最后一段会直接回到第一段；词义配对完成一组后继续循环，缺少“本次完成、正确率、薄弱词、下一建议”的收尾。 | 增加模块结算页，显示本次表现、已获得奖励、待复习词和“继续/返回首页”选择；把完成状态持久化。 |
| P1 | 家长门禁的锁定反馈不够明确 | PIN 冷却到期后页面不一定自动刷新；生物识别按钮在未开启、未录入或不支持的设备上仍可能显示。 | 增加锁定倒计时与定时刷新；仅在“设置已开启且设备可用”时显示生物识别入口；在家长页增加“立即锁定”快捷操作。 |

## 3. 面向小学生的体验优化空间

| 方向 | 建议 | 预期收益 |
|---|---|---|
| 每日学习入口 | 首页增加“今天先做什么”卡片，按到期复习词、未完成新增词批次和下一主线关卡自动给出一个唯一的主按钮。 | 降低选择成本，避免孩子在多个入口间跳转。 |
| 新增词连续学习 | 在内容馆展示每一组的已完成、进行中和未开始状态，并支持选择任意组复练。 | 让 98、82、208 等大词包的学习路径更直观。 |
| 错题反馈 | 错误后不仅提示“再试一次”，还应简短说明错因，并在结算页告诉孩子“兔兔已把这 3 个词放进明天的复习”。 | 增强可解释性，让复习机制被孩子感知。 |
| 对话与口语 | 将角色对话按主题做成 3—5 段小章节，最后给出“对话小演员”结算卡。朗读评分保留为选做，避免因网络或麦克风权限阻塞主学习。 | 强化完成感并降低口语模块的挫败感。 |
| 难度自适应 | 依据最近正确率和待复习词数量，自动把每日目标调整为“1 个巩固关”或“1 个复习 + 1 个新关”。 | 更贴合一年级至初一学生的差异化节奏。 |
| 家长报告 | 将“主线关卡、材料词、阅读、对话、词义配对”拆开显示，再提供总计。 | 让家长知道孩子究竟在哪类活动中投入时间、在哪项技能需要帮助。 |

## 4. 离线、备份与质量保障建议

离线音频包的下载、Wi-Fi 限制、进度、删除与设备发音回退已经形成闭环。当前更适合优化“覆盖范围说明”和“可见状态”：每个等级目前下载的是有限数量的核心词，建议在页面中明确“已下载词数/该等级可下载词数”，并在单词卡上显示“离线可播”标记，避免家长误以为全部新增词已经离线可用。

导出周报与学习档案建议增加一次真机回归：生成后检查文件大小、打开内容和系统分享结果。录音读取与文件 API 也应在 Android 真机上覆盖权限拒绝、录制过长、网络评分失败及退出页面时的资源释放。当前静态检查还存在重复导入、未使用变量和 Hook 依赖警告，建议把 `pnpm lint` 修复为全绿后再设为每次版本保存前的必过项。

## 5. 推荐实施顺序

> **建议先做“稳定性与学习数据一致性包”，不要立刻继续扩展新词或增加新玩法。** 这能直接提升孩子体验、家长报告可信度和后续功能迭代速度。

| 迭代包 | 内容 | 建议工作量 | 验收标准 |
|---|---|---:|---|
| 第一包：稳定性 | 修复条件式 Hook、自动跳题定时器依赖、首页真实题量、清理 lint 错误。 | 小 | `pnpm lint`、类型检查和测试均通过；无效关卡路由可稳定显示回退页。 |
| 第二包：统一复习 | 统一错词事件、补齐材料词/连线/对话的间隔复习写入，并把“到期复习”和“待巩固词”分层展示。 | 中 | 任一模块答错后，可在复习页看到来源明确的巩固任务；报告与复习数一致。 |
| 第三包：统一学习事件 | 让主线关、材料词、微故事、阅读、对话与配对均记录时长、题数、正确率和完成状态。 | 中 | 家长报告能分别和汇总展示所有学习模块，周趋势不遗漏材料学习。 |
| 第四包：完成感与个性化 | 为对话/配对增加结算页和进度，首页提供唯一的智能推荐入口，新增词支持任选组复练。 | 中 | 每个玩法都有明确开始、进行、结算和下一步；首页可一键继续最合适的任务。 |

## 6. 本次检查结果

本次已确认 TypeScript 检查通过、47 项单元测试通过；Android 静态导出在最近一次版本保存前已通过。静态检查发现 1 项实际错误与 26 项警告，其中条件式 Hook 必须优先修复。其余建议均围绕现有功能的衔接和数据完整性，不要求改变已形成的小兔子主题、单机优先、G1—G7 分级和 12 词分组的产品方向。
